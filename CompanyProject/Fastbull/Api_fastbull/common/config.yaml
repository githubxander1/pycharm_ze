#主要作用是作为项目的配置文件，集中管理应用运行时的各种配置参数。这些参数可以包括但不限于日志设置、数据库连接信息、API URLS、环境变量、安全密钥等。
#  通过使用 YAML 这种易于阅读和编写的格式，可以使配置管理更加清晰和高效。

logger:
  name: ITester
  level: DEBUG
  filename: ITester.log
  file: os.path.join(os.path.dirname(__file__), 'logger.log')
#  level: INFO
  format: '%(filename)s-%(lineno)d-%(asctime)s-%(levelname)s-%(message)s'

mongDB:
  host: 192.168.7.72
  port: 27017
  db_name: fastbull_universal_test
  username: fastbull
  password: IOE*2EW#OIWddOPcDWE
